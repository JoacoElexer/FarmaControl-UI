<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
<<<<<<< HEAD
             x:Class="FarmaControl_App.Views.UserModule"
             xmlns:viewmodels="clr-namespace:FarmaControl_App.ViewModels"
             xmlns:models="clr-namespace:FarmaControl_App.Models"
             BackgroundColor="Transparent">
=======
             xmlns:viewmodels="clr-namespace:FarmaControl_App.ViewModels"
             xmlns:models="clr-namespace:FarmaControl_App.Models"
             x:Class="FarmaControl_App.Views.UserModule">
>>>>>>> 8991a21557e0bd584628ceb23cbfdd81816a1d78

    <ContentView.BindingContext>
        <viewmodels:UserViewModel />
    </ContentView.BindingContext>

<<<<<<< HEAD
    <Frame Background="#2B2B33"
           CornerRadius="15"
           Padding="20"
           HasShadow="True"
           HorizontalOptions="FillAndExpand"
           VerticalOptions="FillAndExpand">
        <VerticalStackLayout Spacing="15" Padding="20">
            <Border StrokeShape="RoundRectangle 25"
                    Stroke="#CCCCCC"
                    BackgroundColor="White"
                    Padding="10">
                <Grid ColumnDefinitions="*,Auto">
                    <Entry Grid.Column="0"
                           Placeholder="Buscar"
                           PlaceholderColor="#888888"
                           Text="{Binding SearchText}" TextColor="Black"
                           FontSize="16"
                           VerticalOptions="Center"
                           Keyboard="Text"
                           ClearButtonVisibility="WhileEditing"
                           Margin="0,0,10,0"/>
                    <ImageButton Grid.Column="1"
                                 Source="{FontImageSource Glyph=&#xF002;, FontFamily=FontAwesomeSolid, Color=Black}"
                                 HeightRequest="24"
                                 WidthRequest="24"
                                 VerticalOptions="Center"
                                 HorizontalOptions="End"/>
                </Grid>
            </Border>

            <Label Text="Usuarios"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="White"
                   Margin="0,10,0,0"/>

            <Grid ColumnDefinitions="50,*,*,*,*" Padding="0,10,0,5"
                  BackgroundColor="Transparent">
                <Label Grid.Column="1" Text="Nombre" TextColor="White" FontSize="16" HorizontalOptions="Start" Margin="0,0,0,0"/>
                <Label Grid.Column="2" Text="Usuario" TextColor="White" FontSize="16" HorizontalOptions="Start" Margin="0,0,0,0"/>
                <Label Grid.Column="3" Text="Rol" TextColor="White" FontSize="16" HorizontalOptions="Start" Margin="0,0,0,0"/>
                <Label Grid.Column="4" Text="Email" TextColor="White" FontSize="16" HorizontalOptions="Start" Margin="0,0,0,0"/>
=======
    <ContentView.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="Transparent" Offset="0.0" />
            <GradientStop Color="Transparent" Offset="1.0" />
        </LinearGradientBrush>
    </ContentView.Background>

    <VerticalStackLayout Padding="20" Spacing="20">

        <!-- Buscador -->
        <Border StrokeThickness="0"
                StrokeShape="RoundRectangle 25"
                BackgroundColor="#E2ECFD"
                Padding="0"
                Margin="0,0,0,10" 
                HeightRequest="55">
            <Grid ColumnDefinitions="*, Auto"
                    Padding="0">
                <Entry x:Name="SearchBox"
                        Grid.Column="0"
                        Placeholder="Buscar"
                        TextColor="Black"
                        PlaceholderColor="Gray"
                        BackgroundColor="Transparent"
                        VerticalOptions="Center"
                        FontSize="16" />
                <ImageButton Grid.Column="1"
                     Source="mag.png"
                     BackgroundColor="Transparent"
                     HeightRequest="8"
                     WidthRequest="8"
                     Padding="0"
                     Margin="5,0,0,0"
                     VerticalOptions="Center" />
>>>>>>> 8991a21557e0bd584628ceb23cbfdd81816a1d78
            </Grid>
        </Border>

<<<<<<< HEAD
            <CollectionView ItemsSource="{Binding Users}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:User">
                        <Frame HasShadow="False"
                               CornerRadius="10"
                               Padding="10"
                               Margin="0,5,0,5"
                               BackgroundColor="Transparent">
                            <Grid ColumnDefinitions="Auto,*,*,*,*">
                                <Image Grid.Column="0"
                                       Source="{FontImageSource Glyph=&#xF2BD;, FontFamily=FontAwesomeSolid, Color=White}"
                                       HeightRequest="40"
                                       WidthRequest="40"
                                       VerticalOptions="Center"
                                       Margin="0,0,10,0"/>
                                <Label Grid.Column="1" Text="{Binding Name}" TextColor="White" FontSize="16" VerticalOptions="Center"/>
                                <Label Grid.Column="2" Text="{Binding Username}" TextColor="White" FontSize="16" VerticalOptions="Center"/>
                                <Label Grid.Column="3" Text="{Binding Role}" TextColor="White" FontSize="16" VerticalOptions="Center"/>
                                <Label Grid.Column="4" Text="{Binding Email}" TextColor="White" FontSize="16" VerticalOptions="Center"/>
                            </Grid>
=======

        <!-- Título -->
        <Label Text="Usuarios"
               FontSize="28"
               FontAttributes="Bold"
               TextColor="White"
               Margin="0,0,0,10"/>

        <Button
                Text="Recargar"
                Command="{Binding RefreshCommand}"
                BackgroundColor="#2B25D9"
                TextColor="White"
                CornerRadius="10"
                HeightRequest="40"
                Margin="0,0,0,10"/>

        <!-- Lista de usuarios -->
        <ScrollView Orientation="Horizontal">
            <CollectionView ItemsSource="{Binding Users}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:User">
                        <Frame BackgroundColor="#2B25D9"
                       CornerRadius="10"
                       Padding="10"
                       Margin="0,5"
                       HasShadow="False">
                            <VerticalStackLayout Spacing="10">
                                <Grid ColumnDefinitions="Auto,*,Auto,Auto,Auto">
                                    <Image Grid.Column="0"
                                   Source="clients.png"
                                   HeightRequest="40"
                                   WidthRequest="40"
                                   VerticalOptions="Center"
                                   Margin="0,0,10,0" />
                                    <Label Grid.Column="1" Text="{Binding Name}" TextColor="White" FontSize="14" VerticalOptions="Center" Padding="5"/>
                                    <Label Grid.Column="2" Text="{Binding Username}" TextColor="White" FontSize="14" VerticalOptions="Center" Padding="0,0,30,0"/>
                                    <Label Grid.Column="3" Text="{Binding Role}" TextColor="White" FontSize="14" VerticalOptions="Center" Padding="0,0,30,0"/>
                                    <Label Grid.Column="4" Text="{Binding Email}" TextColor="White" FontSize="14" VerticalOptions="Center"/>
                                </Grid>
                                <!-- Línea divisoria -->
                                <BoxView HeightRequest="1"
                                 BackgroundColor="White"
                                 Opacity="0.3"
                                 HorizontalOptions="FillAndExpand" />
                            </VerticalStackLayout>
>>>>>>> 8991a21557e0bd584628ceb23cbfdd81816a1d78
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
<<<<<<< HEAD
        </VerticalStackLayout>
    </Frame>
=======
        </ScrollView>

    </VerticalStackLayout>
>>>>>>> 8991a21557e0bd584628ceb23cbfdd81816a1d78
</ContentView>